{% extends 'base.html.twig' %}

{% block title %}{{ project.name }}{% endblock %}

{% block body %}
    <h1>{{ project.name }}</h1>
    <a href="{{ path('project_update', {id: project.id}) }}" class="btn btn-light">Edit</a>
    <a id = "deleteButton" href="{{ path('project_delete', {id: project.id}) }}"  class="btn btn-danger delete-article">Delete</a>
    <hr>
    <p>Project id: {{ project.id }}</p>
    <p>Person in charge: {{ project.personInCharge }}</p>
    <p>Keep amount: {{ project.keepAmount }}</p>

    //todo CONNECTIONS
    //todo DATABASES
    //todo DATA

    <hr>
    <a href="{{ path('project_list') }}">Go Back</a>
    {% endblock %}


